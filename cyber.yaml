
---

## 🧪 Data schema (expected columns)

`merged_cyber_incidents.csv` (columns can be missing; app fills safely):
- **incident_id**, **timestamp**, **status**
- **threat_id**, **threat_name**, **threat_type**, **severity**
- **asset_id**, **asset_name**, **asset_type**, **asset_owner_department**, **asset_vulnerability_score**
- **employee_id**, **emp_name**, **emp_department**, **emp_security_training**
- **time_to_resolve_hours**, **data_breached_gb**, **financial_impact_k**
- **description**

The app auto-engineers: `hour`, `dow`, `month`, `is_high_critical` (= severity in {high, critical}).

---

## 🔄 Pipeline (overview)

```mermaid
flowchart LR
    A[CSV: merged_cyber_incidents.csv] --> B[Feature Engineering]
    B --> C[EDA & Dashboards]
    B --> D[ML: Severity Classifier]
    B --> E[ML: High/Critical Classifier]
    B --> F[ML: MTTR Regression]
    B --> G[RAG Index]
    G --> H[Query → Retrieve similar incidents]
    H --> I[Playbook Suggestions]
    C --> J[Streamlit App]
    D --> J
    E --> J
    F --> J
    I --> J
